fastapi==0.104.1
uvicorn==0.24.0.post1
kuzu==0.3.1
pydantic==2.5.2
pydantic-settings==2.1.0
python-multipart==0.0.6
httpx==0.25.1
requests==2.31.0
# Chrome TLS-fingerprint impersonation for read.amazon.com's private Kindle API.
# Runs in-process (no proxy sidecar) - ships prebuilt wheels, no compiler needed.
curl_cffi==0.7.4

